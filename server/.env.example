# ════════════════════════════════════════════════════
#  SERVER — Environment Variables
#  Copy this file to .env and fill in your values.
#  Never commit the actual .env file.
# ════════════════════════════════════════════════════

# ── Server ───────────────────────────────────────────
PORT=8000

# ── CORS ─────────────────────────────────────────────
# Your Vercel frontend URL (no trailing slash)
FRONTEND_URL=https://your-app.vercel.app

# ── Local MySQL (development) ─────────────────────────
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=kanban_db

# ── Railway MySQL (production — set these in Railway dashboard) ──
# Railway injects all MYSQL* vars automatically when you attach a MySQL plugin.
#
# MYSQL_PUBLIC_URL is checked FIRST by connection.ts.
# Use it when connecting from your local machine to Railway's DB via the public proxy.
# MYSQL_PUBLIC_URL=mysql://user:password@host:port/database
#
# MYSQL_URL is the private/internal URL — only reachable inside Railway containers.
# Leave it unset locally; Railway injects it automatically at runtime.
# MYSQL_URL=mysql://root:password@mysql.railway.internal:3306/railway
#
# These individual vars are also injected by Railway's MySQL plugin:
# MYSQLHOST=
# MYSQLUSER=root
# MYSQLPASSWORD=
# MYSQLDATABASE=railway
# MYSQLPORT=3306
