# ════════════════════════════════════════════════════
#  SERVER — Environment Variables
#  Copy this file to .env and fill in your values.
#  Never commit the actual .env file.
# ════════════════════════════════════════════════════

# ── Server ───────────────────────────────────────────
PORT=8000

# ── CORS ─────────────────────────────────────────────
# Your Vercel frontend URL (no trailing slash)
FRONTEND_URL=https://your-app.vercel.app

# ── Local MySQL (development) ─────────────────────────
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=kanban_db

# ── Railway MySQL (production — set these in Railway dashboard) ──
# Railway injects all MYSQL* vars automatically when you attach a MySQL plugin.
# You only need to set them here if running Railway's DB locally via a tunnel.
#
# MYSQL_URL=mysql://root:password@host:port/railway
# MYSQLHOST=
# MYSQLUSER=root
# MYSQLPASSWORD=
# MYSQLDATABASE=railway
# MYSQLPORT=3306
